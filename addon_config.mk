meta:
	ADDON_NAME = ofxModulePictureExporter
	ADDON_DESCRIPTION = Module for ofxModule allows exporting single frames and movies using ofxTextureRecorder.
	ADDON_AUTHOR = brinoausrino
	ADDON_TAGS = "capture" "module"
	ADDON_URL = ofxModulePictureExporter

common:
	ADDON_DEPENDENCIES = ofxModule
	ADDON_DEPENDENCIES += ofxTextureRecorder
